@model VideoRentalApplication.Models.Customer

@{
    ViewBag.Title = "Details";
}

<h2>@Model.Name</h2>

<ul>
    <li><b>Membership Type:</b> @Model.MembershipType.Name</li>

    @if (Model.BirthDate != null)
    { 
    <li><b>Birth Date:</b> @Model.BirthDate</li>
    }
</ul>
